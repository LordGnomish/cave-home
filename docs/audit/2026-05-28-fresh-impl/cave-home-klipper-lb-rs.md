# Coverage matrix — cave-home-klipper-lb-rs

**Declared:** fill=0.00 · adr_justified=(not declared) · honest=(not declared) · scaffold form — port begins phase 1.
**Verified:** 0/0 mapped symbols found in source · 0 test fns · drift: no.

## Status
This crate is in **scaffold state**. The parity.manifest.toml explicitly states: "scaffold + manifest only (real port lands in phase 1)". 

The manifest notes: "Once the port begins, replace this scaffold with [[mapped]] / [[unmapped]] / [[test_port]] tables."

- **Source code:** Only `src/lib.rs` exists, containing a single placeholder comment. No implementation.
- **Tests:** None.
- **Declared coverage:** fill_ratio=0.00, fill_ratio_basis="scaffold + manifest only (real port lands in phase 1)".
- **Upstream:** rancher/klipper-lb (port method: line-by-line, Orchestration Phase 4, ADR-004).

## MAPPED (implemented + claimed)
None — no entries in manifest.

## MISSING / PARTIAL (unmapped + scope_cut, with disposition)
No [[unmapped]] or [[scope_cut]] entries declared. The entire crate is deferred: awaiting phase 1 port.

## Drift notes
None — no mapped symbols declared, so no verification needed. Crate is pre-port scaffold.
