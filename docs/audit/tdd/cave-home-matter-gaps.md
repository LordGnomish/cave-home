# TDD gap analysis — `cave-home-matter`

- **Upstream:** project-chip/connectedhomeip (v1.3.0.0)
- **License class:** permissive
- **Port method:** line-by-line
- **Existing cave tests (agent count):** 78
- **Missing behaviors identified:** 12 (11 strict-TDD-ready)

> Generated by the spec-based gap-analysis pass (read-only). For
> `gpl-agpl-cleanroom` upstreams no upstream source/tests were consulted —
> behaviors are derived from public protocol/spec knowledge only.

**Summary.** Phase 1 commissioner-side Matter 1.3 stack port: 78 tests covering setup-payload parsing, PASE/CASE crypto, fabric/ACL management, 6 cluster clients, and UDP/BLE transports. Missing behaviors: role-state violation handling in CASE, ACL wildcard subject matching for Group auth, error-path tests for secure session derivation edge cases, PBKDF iteration boundary validation, cluster command sequencing constraints, and optional attribute handling in clusters.

## Missing behaviors

### `case-role-violation` — phase-1b — ✅ TDD-ready

CASE session methods (handle_sigma1, handle_sigma2, handle_sigma3, finalize_initiator) should reject calls when session role does not match the handler's expected role

- **Spec:** Matter Core Specification §4.13.2 CASE Handshake: Sigma1/2/3 handlers enforce initiator/responder role constraints
- **Notes:** Code implements role checks with IncorrectState errors but tests (case.rs::tests) only exercise happy path. Missing: responder calling send_sigma1, initiator calling handle_sigma1, mismatched role assertions.

### `acl-group-wildcard-subjects` — phase-1b — ✅ TDD-ready

AccessControl::check should allow Group auth_mode entries with empty subjects (wildcard group), distinct from CASE require-at-least-one behavior

- **Spec:** Matter Core Specification §9.10.7 ACL Entry Structure: Group entries support subjectless wildcard configuration
- **Notes:** Entry::validate checks Group subjects.is_empty() correctly but tests (acl.rs::tests) do not verify that Group wildcard + Operate privilege authorizes any group-authenticated request.

### `case-fabric-mismatch-detail` — phase-1b — ✅ TDD-ready

CASE handle_sigma1 should reject Sigma1 if initiator_fabric_id does not match responder's fabric_id, returning Fabric error with descriptive message

- **Spec:** Matter Core Specification §4.13.2 CASE Handshake: Fabric validation during Sigma1 processing
- **Notes:** Code returns error (case.rs line 124-128) but no test verifies rejection with mismatched fabric IDs. Tests (case.rs::tests) only exercise matching fabric.

### `ota-apply-update-invalid-version` — phase-1b — ✅ TDD-ready

OtaRequestor::apply_update should reject updates when available_version is None or unset

- **Spec:** Matter Core Specification §11.19.3 OTA Update Requestor: version availability prerequisite
- **Notes:** Code path exists (ota.rs::apply_update) but tests do not verify error when available_version is None. Current tests only exercise happy-path with version set.

### `color-control-saturation-edge` — phase-1b — ✅ TDD-ready

ColorControlClient::move_to_hue_and_saturation should accept saturation value 254 (max) but reject 255 and above

- **Spec:** Matter Core Specification §5.2.5.4 Color Control Cluster: saturation attribute range [0-254]
- **Notes:** Code validates saturation > 254 (color_control.rs line 47-50) correctly. Tests do not verify edge case (saturation == 254 accepted, 255 rejected).

### `door-lock-pin-non-ascii` — phase-1b — ✅ TDD-ready

DoorLockClient PIN validation should ensure all bytes are ASCII digit characters [0-9]

- **Spec:** Matter Core Specification §5.2.6.1 Door Lock Cluster: PIN code format must be numeric
- **Notes:** Code path exists (door_lock.rs line 79) verifying ASCII digits. Tests check length bounds but do not verify rejection of non-digit characters (e.g., 'a', symbols).

### `fabric-index-uniqueness-on-add` — phase-1b — ✅ TDD-ready

FabricTable::add_pending should reject duplicate pending operations (second add_pending before commit)

- **Spec:** Matter Core Specification §6.3 Fabric Management: pending fabric atomic staging constraint
- **Notes:** Code checks s.pending.is_some() and returns IncorrectState error (fabric.rs line 99-101). Tests verify this path exists but coverage could be explicit: add_pending twice, verify second rejects.

### `setup-payload-version-field-valid-range` — phase-1b — ✅ TDD-ready

SetupPayload version field should validate that version value is in range [0, VERSION_MAX] (0-7)

- **Spec:** Matter Core Specification §5.4.2.2 Setup Payload Encoding: version field is 3 bits (0-7)
- **Notes:** Version is u8; parsing does extract 3 bits. No test verifies rejection of payload with version > 7 after parsing. Happy-path tests exist but edge case missing.

### `acl-multiple-targets-any-match` — phase-1b — ✅ TDD-ready

AccessControl::check with multi-target ACL entry should authorize if ANY target in the entry's targets vector matches the request path

- **Spec:** Matter Core Specification §9.10.7 ACL Entry: target list matching is OR (any match permits access)
- **Notes:** Code implements any() matching (acl.rs line 182-183) correctly. Tests do not verify multi-target entry authorization: entry with targets=[{cluster:A}, {cluster:B}] should allow both clusters.

### `pbkdf-high-iteration-bound` — phase-2 — ✅ TDD-ready

PbkdfParameters validation should reject iteration counts > ITERATION_COUNT_MAX (100k)

- **Spec:** Matter Core Specification §4.3.1 PASE Setup: iteration count upper bound validation
- **Notes:** Code validates 1k..100k range but tests (pase.rs::tests) only check low boundary (test_pbkdf_param_validate_rejects_low_iterations). No test for iteration count exceeding 100_000.

### `thread-dataset-pan-id-validation` — phase-2 — ✅ TDD-ready

ThreadOperationalDataset validation should reject PAN IDs that are reserved (e.g., 0xFFFF broadcast or 0x0000 unset)

- **Spec:** Matter Core Specification §11.8.6.2.3 NetworkCommissioning Thread: IEEE 802.15.4 PAN ID constraints
- **Notes:** Code does not validate pan_id bounds. Spec requires valid IEEE 802.15.4 16-bit PAN IDs (0x0001..0xFFFE). No test enforces this constraint.

### `commissioner-next-node-id-overflow` — permanent — ⏸ not-yet (needs impl-deps / test-after)

Commissioner should handle node ID allocation overflow or exhaustion (when next_node_id_inc wraps or reaches u64 max)

- **Spec:** Matter Core Specification §4.4.1 Node ID Assignment: implementation-defined allocation policy
- **Notes:** Already implemented (commissioner.rs uses saturating_add in pair_device), test-after only: behavior is stubbed but not explicitly tested. Would need test of repeated pairing to trigger saturation boundaries.
