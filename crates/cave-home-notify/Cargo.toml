[package]
name              = "cave-home-notify"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace   = true
description = "cave-home-notify — notification routing brain (priority scale, validated pub/sub topics, subscription/channel routing, dedup window + per-topic token-bucket rate limit, EN/DE/TR grandma-friendly rendering); first-party ntfy-class domain model, std-only, no transports (ADR-021)"

[package.metadata.cave-home]
upstream    = "none (first-party ntfy-class semantics)"
port-method = "spec-based (self-hosted notification semantics) + first-party (routing/throttle/i18n logic)"
adr         = "ADR-021"

[lints]
workspace = true
