# cave-home-audio-snapcast — parity manifest scaffold.
#
# Charter v2 G4: every crate ships a parity.manifest.toml. This is the
# scaffold form — fill_ratio is bumped as the line-by-line port lands.
# cave-home-audio-snapcast — multi-room audio synchronisation (Snapcast wire protocol); reference: badaix/snapcast (GPL-3.0, source NOT read); port method: CLEAN-ROOM from public protocol docs (ADR-020, Charter §6.1)

[crate]
name             = "cave-home-audio-snapcast"
upstream_repo    = "badaix/snapcast (GPL-3.0, reference only, source NOT read)"
port_method      = "clean-room"
spec_sources     = [
    "Snapcast public protocol documentation (docs/ tree of upstream repo, documentation files only — not source)",
]
ported_at        = "2026-05-25"
linux_min_kernel = "7.1"

[ratios]
fill_ratio              = 0.00
fill_ratio_basis        = "scaffold + manifest only (real port lands in phase 1)"
fill_ratio_full_module  = 0.00
test_port_ratio         = 0.00

# Once the port begins, replace this scaffold with [[mapped]] / [[unmapped]] /
# [[test_port]] tables (see crates/cave-home-core/parity.manifest.toml for shape).

