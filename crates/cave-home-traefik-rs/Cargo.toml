[package]
name              = "cave-home-traefik-rs"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace   = true
description = "cave-home-traefik-rs — ingress routing-decision core (K3s default; optional in cave-home); upstream: traefik/traefik (MIT); port method: spec-based behavioural reimplementation of documented routing semantics (Orchestration Phase 4, ADR-004)"

[package.metadata.cave-home]
upstream    = "traefik/traefik"
port-method = "spec-based"
phase       = "orchestration-phase-4"
optional    = true

# std-only by design: the routing-decision core needs no external crates.
# The actual TCP/TLS listener + ACME + provider watchers (phase-1b) will pull
# in async/TLS deps when they land; the decision core stays dependency-free.

[lints]
workspace = true
