[package]
name              = "cave-home-solar-sunspec"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace   = true
description = "cave-home-solar-sunspec — vendor-agnostic SunSpec Modbus register decoder for solar inverter monitoring (SMA / Fronius / SolarEdge / Huawei / Goodwe / Kostal); spec: SunSpec Alliance public, port method: spec-based"

[package.metadata.cave-home]
upstream    = "SunSpec Alliance public model spec"
port-method = "spec-based"
spec-source = "SunSpec Alliance — public model definitions (1, 101-103, 111-113)"
spec-url    = "https://sunspec.org/sunspec-information-model-specifications/"

# Phase-1 MVP is pure decode logic — std-only, no external crates, no network
# or hardware. The live Modbus-TCP/RTU transport and device polling are
# Phase-1b (see parity.manifest.toml [[unmapped]]).

[lints]
workspace = true
