# cave-home-dns-adguard — parity manifest scaffold.
#
# Charter v2 G4: every crate ships a parity.manifest.toml. This is the
# scaffold form — fill_ratio is bumped as the line-by-line port lands.
# cave-home-dns-adguard — DNS ad-blocking + parental controls (AdGuard Home-class); reference: AdguardTeam/AdGuardHome (GPL, source NOT read); port method: CLEAN-ROOM from AdGuard admin-API public docs + OpenAPI spec (ADR-022, Charter §6.1)

[crate]
name             = "cave-home-dns-adguard"
upstream_repo    = "AdguardTeam/AdGuardHome (GPL, reference only, source NOT read)"
port_method      = "clean-room"
spec_sources     = [
    "AdGuard Home admin-API public docs (github.com/AdguardTeam/AdGuardHome/wiki) + OpenAPI spec",
]
ported_at        = "2026-05-25"
linux_min_kernel = "7.1"

[ratios]
fill_ratio              = 0.00
fill_ratio_basis        = "scaffold + manifest only (real port lands in phase 1)"
fill_ratio_full_module  = 0.00
test_port_ratio         = 0.00

# Once the port begins, replace this scaffold with [[mapped]] / [[unmapped]] /
# [[test_port]] tables (see crates/cave-home-core/parity.manifest.toml for shape).

