[package]
name              = "cave-home-knx"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace   = true
description = "cave-home-knx — KNX datapoint-type (DPT) codec + group-telegram model: pure-logic, std-only engine built from the public KNX standard (xknx MIT public behavior referenced; KNXd strong-copyleft source NOT read — clean-room recusal for the deferred gateway)"

[package.metadata.cave-home]
upstream    = "Public KNX standard (DPT tables, cEMI/APCI framing); XKNX/xknx (MIT) public behavior referenced; KNXd (strong copyleft, NOT read — clean-room recusal)"
port-method = "spec-based (public KNX DPT/APCI tables) + xknx-MIT public-behavior reference"
adr         = "ADR-011"

[package.metadata.clean-room]
component         = "gateway (knxd-equivalent, deferred to Phase 1b)"
reason            = "KNXd is strong copyleft — license-incompatible with cave-home Apache-2.0"
spec-source       = "KNX Association public service-code table + KNX/IP framing"
prohibited-source = ["KNXd (selfbus/knxd)", "Wireshark KNX dissector"]
attestation       = "No KNXd or Wireshark KNX dissector source consulted at any point."

# Phase-1 MVP is pure logic: std-only, no external crates, no network/hardware.
# The KNXnet/IP, TPUART and gateway-daemon transports are Phase-1b (see manifest).
[dependencies]

[dev-dependencies]

[lints]
workspace = true
