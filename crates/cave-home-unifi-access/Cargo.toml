[package]
name              = "cave-home-unifi-access"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace   = true
description = "cave-home-unifi-access — UniFi Access port (door locks, hub status, access events); upstream: home-assistant/core UniFi Access integration + Ubiquiti Access REST API; port method: line-by-line (ADR-009)"

[package.metadata.cave-home]
upstream    = "home-assistant/core (UniFi Access) + Ubiquiti Access REST"
port-method = "line-by-line"
adr         = "ADR-009"

[package.metadata.upstream]
repo    = "home-assistant/core"
release = "2026.5.2"
sha     = "456202325ac48549bd3c895dc3e69ecd3e2ba6a4"
subpath = "homeassistant/components/unifi_access"

# Phase-1 MVP is pure, std-only domain logic: the door-control model + access
# policy engine. No external crates, no network/hardware/crypto. The transport
# and real crypto deps land in Phase 1b (see parity.manifest.toml, ADR-009).
[dependencies]

[dev-dependencies]

[lints]
workspace = true
