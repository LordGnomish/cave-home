[package]
name              = "cave-home-unifi-network"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
repository.workspace   = true
description = "cave-home-unifi-network — UniFi Network domain model + control engine (devices, clients, control ops, presence, connectivity summary); spec/semantics-port of HA unifi + the public UniFi Network API shape (ADR-009)"

[package.metadata.cave-home]
upstream    = "home-assistant/core (unifi integration) + Ubiquiti public local Network API"
port-method = "spec-based (HA semantics + public API shape)"
adr         = "ADR-009"

# Phase 1 ships a pure-logic, std-only domain model + control engine. The
# UniFi controller REST + WebSocket transport is network-bound and deferred to
# Phase 1b (see parity.manifest.toml [[unmapped]]); it brings its own async /
# serde deps when it lands, so none are needed here yet.

[dependencies]

[dev-dependencies]

[lints]
workspace = true
