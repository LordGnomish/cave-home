# cave-home-lighting-wled — parity manifest scaffold.
#
# Charter v2 G4: every crate ships a parity.manifest.toml. This is the
# scaffold form — fill_ratio is bumped as the line-by-line port lands.
# cave-home-lighting-wled — WLED addressable-LED firmware client; reference: WLED upstream (treated as copyleft, source NOT read); port method: CLEAN-ROOM from WLED JSON API + UDP realtime public docs (ADR-014, Charter §6.1)

[crate]
name             = "cave-home-lighting-wled"
upstream_repo    = "Aircoookie/WLED (treated as copyleft, source NOT read)"
port_method      = "clean-room"
spec_sources     = [
    "WLED JSON API public docs (kno.wled.ge) + UDP realtime protocol public spec + WS2812/SK6812 NeoPixel public timing diagrams",
]
ported_at        = "2026-05-25"
linux_min_kernel = "7.1"

[ratios]
fill_ratio              = 0.00
fill_ratio_basis        = "scaffold + manifest only (real port lands in phase 1)"
fill_ratio_full_module  = 0.00
test_port_ratio         = 0.00

# Once the port begins, replace this scaffold with [[mapped]] / [[unmapped]] /
# [[test_port]] tables (see crates/cave-home-core/parity.manifest.toml for shape).

