//! cave-home-kine-rs — placeholder. See ADR-004, ROADMAP M4 (Orchestration Phase 3).
//!
//! Speaks the etcd v3 gRPC API with a SQLite (or Postgres) backend.
//! This is the trick that makes a Kubernetes control plane fit in one
//! single binary — without kine the control plane needs a separate etcd.
